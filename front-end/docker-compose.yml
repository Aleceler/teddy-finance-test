services:
  front-end:
    image: node:22-alpine
    container_name: teddy-open-finance-front-end
    working_dir: /workspace
    volumes:
      - ..:/workspace
    ports:
      - '4200:4200'
    command: npx nx dev front-end -- --host 0.0.0.0
    environment:
      CHOKIDAR_USEPOLLING: 'true'
